import { initializeNavigation } from './components/navigation.js';

/**
 * Wrapper to initialize the shared mobile navigation component.
 */
export function initNavigation() {
  initializeNavigation();
}
